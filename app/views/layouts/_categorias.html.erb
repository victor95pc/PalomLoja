<% if categorias == selected %>
    <% id = 'categoria-selected' %>
<% else %>
    <% id = categorias.nome %>
<% end %>

<%= link_to(categorias.nome, categoria_produtos_path(categorias.nome), class: 'list-group-item', id: id) %>
